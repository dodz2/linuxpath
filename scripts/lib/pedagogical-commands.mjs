import { createBrowserPedagogicalCommands } from './browser-terminal-runtime.mjs';

// Adaptateur Node : le registre exécutable est celui livré au navigateur.
export const pedagogicalCommands = createBrowserPedagogicalCommands();
